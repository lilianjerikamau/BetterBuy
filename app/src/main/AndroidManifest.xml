<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.betterbuy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BetterBuy"
        tools:replace="android:icon,android:theme,android:allowBackup,label">
        <activity
            android:name=".CartActivity"
            android:label="@string/title_activity_cart"
            android:theme="@style/Theme.BetterBuy.NoActionBar"></activity>
        <activity android:name=".ProductDetailsActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".TweetsActivity" />
        <activity android:name=".MyApplication" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.BetterBuy.NoActionBar" />
        <activity android:name=".AdminCategoryActivity" /> <!-- <activity android:name=".TwitterLoginActivity" /> -->
        <activity android:name=".AdminAddNewProductActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5292422000574799~xxxxxxxxxx" />
    </application>

</manifest>